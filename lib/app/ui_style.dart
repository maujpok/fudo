import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primaryColor = Color(0XFFFF5023);
  static const Color secondaryColor = Color(0xFFE0E0E0);
  static const Color defaultTextColor = Color.fromARGB(237, 0, 0, 0);
  static const Color errorColor = Colors.red;
  static const Color successColor = Colors.green;
  static const Color warningColor = Colors.yellow;
}

class AppSizes {
  AppSizes._();

  static const double minPadding = 8.0;
  static const double defaultPadding = 16.0;
  static const double maxPadding = 32.0;

  static const double defaultBorderRadius = 8.0;
  static const double maxBorderRadius = 16.0;

  static const double defaultText = 12.0;
  static const double maxText = 16.0;
  static const double bigText = 24.0;

}